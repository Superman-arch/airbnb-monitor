# Core Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Database
asyncpg==0.29.0
sqlalchemy==2.0.23
alembic==1.13.0
psycopg2-binary==2.9.9

# Redis & Caching
redis==5.0.1
aiocache==0.12.2
hiredis==2.2.3

# WebSocket & Real-time
websockets==12.0
python-socketio==5.10.0

# Computer Vision & ML
opencv-python==4.8.1.78
numpy>=1.26.1  # Updated for onnxruntime-gpu compatibility
# Pillow==10.1.0  # Pre-installed in L4T
ultralytics==8.0.200  # YOLOv8
# torch==2.1.0  # Pre-installed in L4T
# torchvision==0.16.0  # Pre-installed in L4T
onnx==1.15.0
# onnxruntime-gpu==1.16.3  # Pre-installed in L4T

# Tracking dependencies
lap==0.4.0  # Linear Assignment Problem solver for ByteTracker
scipy>=1.9.0  # Required for tracking algorithms
filterpy==1.4.5  # Kalman filters for tracking

# Video Streaming
aiortc==1.5.0  # WebRTC
av==10.0.0  # Video processing - Compatible with aiortc 1.5.0

# Monitoring & Logging
# prometheus-client==0.19.0  # Pre-installed in L4T
structlog==23.2.0
# python-json-logger==2.0.7  # Pre-installed in L4T
sentry-sdk==1.38.0

# Task Queue
celery==5.3.4
kombu==5.3.4
flower==2.0.1  # Celery monitoring

# API Documentation
pydantic==2.5.2
pydantic-settings==2.1.0

# Security
cryptography==41.0.7
bcrypt==4.1.2  # Fix bcrypt version issue
pyotp==2.9.0  # 2FA support
httpx==0.25.2
email-validator==2.1.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Development Tools
black==23.12.0
flake8==6.1.0
mypy==1.7.1
pre-commit==3.6.0

# Utilities
pendulum==3.0.0  # Better datetime
orjson==3.9.10  # Fast JSON
aiofiles==23.2.1
tenacity==8.2.3  # Retry logic
# cachetools==5.3.2  # Pre-installed in L4T

# Jetson Specific (optional - install separately if needed)
# jetson-stats==4.2.3
# tensorrt==8.6.1  # Install via NVIDIA SDK Manager